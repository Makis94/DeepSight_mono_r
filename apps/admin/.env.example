# Point this dev server's own origin, not apps/api directly — vite.config.ts proxies
# /admin/* to localhost:3001 so the admin session cookie stays same-origin (see the
# server.proxy comment in vite.config.ts for why).
VITE_API_URL=http://localhost:5174
